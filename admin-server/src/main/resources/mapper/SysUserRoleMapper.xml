<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.saucesubfresh.admin.server.mapper.SysUserRoleMapper">

  <select id="loadUserRolesByUserId" resultType="com.saucesubfresh.admin.server.entity.SysRoleDO">
    SELECT sr.* FROM open_admin_user_role sur
    LEFT JOIN open_admin_role sr on sur.role_id = sr.id
    WHERE sur.user_id = #{userId}
    AND sr.id IS NOT NULL
    GROUP BY sr.id
  </select>
</mapper>