<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.pro.admin.server.mapper.SysUserRoleMapper">

  <select id="loadUserRolesByUserId" resultType="com.pro.admin.server.entity.SysRoleDO">
    SELECT sr.* FROM fresh_sys_user_role sur
    LEFT JOIN fresh_sys_role sr on sur.role_id = sr.id
    WHERE sur.user_id = #{userId}
        AND sr.id IS NOT NULL
    GROUP BY sr.id
  </select>
</mapper>